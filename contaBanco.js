nomeDoObjeto.nomedoMetodo = nomedafuncao;

var meuObjeto = {
meuMetodo: function(parametros) {}
};

objeto.nomedoMetodo(parametros);

function Banco(saldo, deposito, saque, numeroConta){
this.saldo = saldo;
this.deposito = deposito;
this.saque = saque;
this.numeroConta = numeroConta
}
